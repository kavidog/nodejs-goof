name: hello

on: workflow_dispatch

jobs:
  sample:
    runs-on: ubuntu-latest
    name: Bsides Mtl
    steps:
      - run: |
            id
            uname -a
            env
            echo "Hello BSides!"
